version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rodelas-next-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
